<Window x:Class="TaskManager.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:TaskManager"
        Title="Менеджер задач" Height="600" Width="700"
        WindowStartupLocation="CenterScreen"
        Background="#F3F4F6"
        x:Name="MainWindowRoot">

    <Window.DataContext>
        <local:MainViewModel />
    </Window.DataContext>

    <Window.Resources>
        <!-- Стиль для основной кнопки -->
        <Style TargetType="Button" x:Key="PrimaryButton">
            <Setter Property="Background" Value="#3B82F6"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="20,10"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="6" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#2563EB"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Стиль для кнопки удаления -->
        <Style TargetType="Button" x:Key="DangerButton">
            <Setter Property="Background" Value="#FEE2E2"/>
            <Setter Property="Foreground" Value="#DC2626"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="4" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#FECACA"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Стиль для поля ввода -->
        <Style TargetType="TextBox" x:Key="ModernTextBox">
            <Setter Property="Background" Value="White"/>
            <Setter Property="Foreground" Value="#1F2937"/>
            <Setter Property="BorderBrush" Value="#D1D5DB"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="12,10"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="6">
                            <ScrollViewer x:Name="PART_ContentHost" Margin="0" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsFocused" Value="True">
                    <Setter Property="BorderBrush" Value="#3B82F6"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Заголовок -->
        <TextBlock Grid.Row="0" Text="Мои задачи" FontSize="28" FontWeight="Bold" Foreground="#111827" Margin="0,0,0,10"/>

        <!-- Панель добавления задачи -->
        <Grid Grid.Row="0" Margin="0,40,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBox Grid.Column="0" 
                     Style="{StaticResource ModernTextBox}"
                     Text="{Binding NewTaskText, UpdateSourceTrigger=PropertyChanged}" 
                     VerticalContentAlignment="Center">
                <TextBox.InputBindings>
                    <KeyBinding Key="Enter" Command="{Binding AddTaskCommand}" />
                </TextBox.InputBindings>
            </TextBox>

            <Button Grid.Column="1" 
                    Content="+ Добавить" 
                    Command="{Binding AddTaskCommand}" 
                    Margin="12,0,0,0" 
                    Style="{StaticResource PrimaryButton}"/>
        </Grid>

        <!-- Список задач -->
        <ListView Grid.Row="1" 
                  ItemsSource="{Binding Tasks}" 
                  BorderThickness="0"
                  Background="Transparent"
                  ScrollViewer.HorizontalScrollBarVisibility="Disabled">

            <!-- Убираем стандартную подсветку и отступы ListViewItem -->
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="BorderThickness" Value="0"/>
                    <Setter Property="Padding" Value="0,0,0,10"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="ListViewItem">
                                <ContentPresenter />
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </ListView.ItemContainerStyle>

            <ListView.ItemTemplate>
                <DataTemplate>
                    <!-- Карточка задачи -->
                    <Border Background="White" CornerRadius="8" Padding="15" BorderBrush="#E5E7EB" BorderThickness="1">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <!-- CheckBox -->
                            <CheckBox Grid.Column="0" 
                                      IsChecked="{Binding IsCompleted}" 
                                      VerticalAlignment="Center" 
                                      Margin="0,0,15,0"
                                      Cursor="Hand"/>

                            <!-- Текст задачи -->
                            <TextBlock Grid.Column="1" 
                                       Text="{Binding Title}" 
                                       VerticalAlignment="Center" 
                                       FontSize="15"
                                       FontWeight="Medium"
                                       Foreground="#374151"
                                       TextWrapping="Wrap">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsCompleted}" Value="True">
                                                <Setter Property="TextDecorations" Value="Strikethrough"/>
                                                <Setter Property="Foreground" Value="#9CA3AF"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>

                            <!-- Дата и время создания -->
                            <TextBlock Grid.Column="2" 
                                       Text="{Binding CreatedAt, StringFormat='{}{0:dd.MM HH:mm}'}" 
                                       VerticalAlignment="Center" 
                                       Foreground="#9CA3AF" 
                                       FontSize="12"
                                       Margin="0,0,15,0"/>

                            <!-- Кнопка удаления -->
                            <Button Grid.Column="3" 
                                    Content="Удалить" 
                                    Command="{Binding DataContext.DeleteTaskCommand, ElementName=MainWindowRoot}" 
                                    CommandParameter="{Binding}" 
                                    Style="{StaticResource DangerButton}"/>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <!-- Информационная панель -->
        <Border Grid.Row="2" 
                Background="White" 
                CornerRadius="8" 
                Padding="20" 
                Margin="0,20,0,0"
                BorderBrush="#E5E7EB"
                BorderThickness="1">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <StackPanel Orientation="Horizontal" Margin="0,0,30,0">
                    <TextBlock Text="Всего:" FontSize="14" Foreground="#6B7280" Margin="0,0,8,0"/>
                    <TextBlock Text="{Binding TotalCount}" FontWeight="Bold" FontSize="16" Foreground="#111827"/>
                </StackPanel>

                <StackPanel Orientation="Horizontal" Margin="0,0,30,0">
                    <TextBlock Text="Активных:" FontSize="14" Foreground="#6B7280" Margin="0,0,8,0"/>
                    <TextBlock Text="{Binding ActiveCount}" FontWeight="Bold" FontSize="16" Foreground="#10B981"/>
                </StackPanel>

                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="Выполненных:" FontSize="14" Foreground="#6B7280" Margin="0,0,8,0"/>
                    <TextBlock Text="{Binding CompletedCount}" FontWeight="Bold" FontSize="16" Foreground="#3B82F6"/>
                </StackPanel>
            </StackPanel>
        </Border>
    </Grid>
</Window>